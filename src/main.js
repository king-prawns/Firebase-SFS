import '../static/styles/main.scss';

const $container = document.getElementById('container');
const $title = document.createElement('h1');
$container.appendChild($title);
$title.innerText = 'Hello Firebase!';
